ANTHROPIC_API_KEY=
AI_PROVIDER=anthropic
NEXT_PUBLIC_APP_NAME=BearAi

# Optional — only needed if AI_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

# Optional — override the default Anthropic model
ANTHROPIC_MODEL=claude-sonnet-4-5
